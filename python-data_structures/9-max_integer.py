#!/usr/bin/python3
def max_integer(my_list=[]):
    if not my_list:
        return None
    max_val = my_list[0]
    for max_val in my_list:
        if max_val < val:
            max_val = val
    return max_val
